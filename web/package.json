{
  "name": "lower-off",
  "version": "0.0.1",
  "description": "Random Sport Lower-off Scenarios",
  "repository": {
    "type": "git",
    "url": "https://github.com/sparksp/lower-off.git"
  },
  "bugs": {
    "url": "https://github.com/sparksp/lower-off/issues"
  },
  "homepage": "https://github.com/sparksp/lower-off#readme",
  "scripts": {
    "prebuild:api": "if [ -z $URL ]; then echo \"ERROR: No URL set\"; exit 1; fi",
    "build:api": "hugo -b $URL/api/ -d ../web/dist/api/ -s ../api/",
    "build:web": "parcel build entry.html --out-file index.html",
    "prebuild": "rimraf dist",
    "build": "run-s build:*",
    "dev:api": "hugo -b http://localhost:1234/api/ -d ../web/dist/api -s ../api",
    "dev:web": "parcel entry.html --out-file index.html",
    "dev": "run-s dev:*",
    "lint:format": "find src tests review/src -name *.elm ! -name Tailwind.elm | xargs elm-format --yes",
    "lint:review": "elm-review",
    "lint": "run-s lint:*",
    "test": "elm-test"
  },
  "author": "Phill Sparks <me@phills.me.uk>",
  "license": "MIT",
  "devDependencies": {
    "elm": "^0.19.1-3",
    "elm-format": "^0.8.4",
    "elm-hot": "^1.1.5",
    "elm-json": "^0.2.8",
    "elm-review": "^2.3.3",
    "elm-test": "^0.19.1-revision4",
    "npm-run-all": "^4.1.5",
    "parcel": "^1.12.4",
    "rimraf": "^3.0.2",
    "src": "^1.1.2"
  }
}
